# Private key for the wallet that holds USDC on Arbitrum
PRIVATE_KEY=0xYourPrivateKeyHere

# Amount of USDC to bridge (in human-readable units, e.g. 10 for 10 USDC)
AMOUNT_USDC=10

# Optional: recipient address on Base (defaults to depositor)
# RECIPIENT=0xRecipientAddress
